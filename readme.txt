-------------------------------------------------
--  Consulta de Items por vendedor
--  Solo aplica a MLA
--  para python 3.7+
-------------------------------------------------

input: seller_id
output: txt separado por ; conteniendo: id, title, category_id, name

Para ejecutar, desde su consola hacer:
python demo_mp <seller_id>

para mutiples seller solo incluir en un bucle

